# Stabilizer Codes (Advanced)

Surface codes belong to the stabilizer code family: errors are detected by
measuring "stabilizer" operators that commute with the encoded logical
state but reveal error syndromes without collapsing the logical
information itself. Decoding (finding the most likely error given a
syndrome) is typically done via minimum-weight perfect matching.
