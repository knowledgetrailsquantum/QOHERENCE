# Statistical Rigor in Quantum Benchmarks (Expert)

Quantum measurement is inherently probabilistic — a single run tells you
little. Use confidence intervals (e.g. bootstrap resampling over shots),
report shot counts explicitly, and be wary of cherry-picking best-of-N runs
when comparing algorithms or hardware backends in publishable results.
